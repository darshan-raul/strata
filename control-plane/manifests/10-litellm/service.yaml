apiVersion: v1
kind: Service
metadata:
  name: litellm
  namespace: strata
  labels:
    app: litellm
    app.kubernetes.io/part-of: strata
spec:
  type: ClusterIP
  selector:
    app: litellm
  ports:
    - name: http
      port: 4000
      targetPort: http
      protocol: TCP
