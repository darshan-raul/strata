apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-service
  namespace: {{ include "Strata.namespace" . }}
spec:
  replicas: {{ .Values.auditService.replicaCount }}
  selector:
    matchLabels:
      app: audit-service
  template:
    metadata:
      labels:
        app: audit-service
    spec:
      containers:
        - name: audit-service
          image: "{{ .Values.auditService.image.repository }}:{{ .Values.auditService.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.auditService.service.port }}
          env:
            - name: DB_DSN
              value: {{ include "Strata.postgres.url" . }}
            - name: NATS_URL
              value: {{ include "Strata.nats.url" . }}
            - name: PORT
              value: {{ .Values.auditService.service.port | quote }}
          resources:
            {{- toYaml .Values.auditService.resources | nindent 10 }}