apiVersion: v1
kind: Service
metadata:
  name: scorecard-service
  namespace: {{ include "Strata.namespace" . }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.scorecardService.service.port }}
      targetPort: {{ .Values.scorecardService.service.port }}
  selector:
    app: scorecard-service