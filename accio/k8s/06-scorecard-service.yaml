apiVersion: apps/v1
kind: Deployment
metadata:
  name: scorecard-service
  namespace: accio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scorecard-service
  template:
    metadata:
      labels:
        app: scorecard-service
    spec:
      containers:
        - name: scorecard-service
          image: localhost:5000/scorecard-service:latest
          ports:
            - containerPort: 8083
          env:
            - name: DB_DSN
              value: "postgres://accio:accio_password@postgres:5432/accio?sslmode=disable"
            - name: NATS_URL
              value: "nats://nats:4222"
            - name: PORT
              value: "8083"
          resources:
            limits:
              cpu: "0.5"
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: scorecard-service
  namespace: accio
spec:
  type: ClusterIP
  ports:
    - port: 8083
      targetPort: 8083
  selector:
    app: scorecard-service