apiVersion: v1
kind: Service
metadata:
  name: authelia
  namespace: {{ include "accio.namespace" . }}
spec:
  type: ClusterIP
  ports:
    - port: 9091
      targetPort: 9091
  selector:
    app: authelia