apiVersion: v1
kind: Service
metadata:
  name: scorecard-service
  namespace: {{ include "accio.namespace" . }}
  labels:
    {{- include "accio.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.scorecardService.service.port }}
      targetPort: {{ .Values.scorecardService.service.port }}
  selector:
    {{- include "accio.selectorLabels" . | nindent 4 }}
    component: scorecard-service